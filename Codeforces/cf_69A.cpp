#include <bits/stdc++.h>

#define read(x) freopen("x" , "r" , stdin)
#define write(x) freopen("x", "w" , stdout)

using namespace std;
typedef long long int lli;

int main ()
{
    int n,sum;
    bool flag = true;

    cin >> n;

    int ara[n];

    for(int i=0;i<n;i++){
        int a,b,c;
        cin >> a >> b >> c;
        int sum = a+b+c;

    }

    return 0;
}
